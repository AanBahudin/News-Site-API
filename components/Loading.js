const Loading = () => {
    return (
        <section className="h-[100vh] items-center justify-center flex">
            <h1 className="uppercase text-4xl font-semibold text-blue">Loading ...</h1>
        </section>
    )
}

export default Loading